<?php
// Exit if accessed directly
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

/**
 * Placeholder encryption function (disabled for testing)
 */
function sharering_link_encrypt( $data ) {
    // For testing, return data unmodified
    return $data;
}

/**
 * Placeholder decryption function (disabled for testing)
 */
function sharering_link_decrypt( $data ) {
    // For testing, return data unmodified
    return $data;
}
